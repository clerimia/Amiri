{
  "name": "amiri",
  "version": "0.0.0",
  "private": true,
  "description": "Amiri -- 以 Pi 为 runtime 基座的个人 Agent（TypeScript）",
  "type": "module",
  "scripts": {
    "ingest:blog": "cd src/extensions/rag && npm run ingest:blog",
    "ingest:interview": "cd src/extensions/rag && npm run ingest:interview",
    "test": "cd src/extensions/rag && npm run test",
    "test:watch": "cd src/extensions/rag && npm run test:watch",
    "typecheck": "cd src/extensions/rag && npm run typecheck"
  }
}
